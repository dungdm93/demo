name: 'Hello World'
description: 'Greet someone and record the time'
runs:
  using: 'node20'
  main: 'index.js'
  # pre-if: always()
  # pre-if: inputs.pre == github.action
  pre: pre.js
  # post-if: always()
  # post-if: inputs.post == github.action
  post: post.js
